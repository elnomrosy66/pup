@model IEnumerable<H_Invoice.Models.validationErrors>

<table class="table">
    <tr>
        <th>
            الرسالة
        </th>
        <th>
            المرجع
        </th>


        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr style="color:@item.Color">
            <td>
                @Html.DisplayFor(modelItem => item.Message)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reference)
            </td>

        </tr>
    }

</table>
